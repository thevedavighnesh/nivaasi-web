# Railway Production Environment Variables
# Copy these to your Railway dashboard under Variables

NODE_ENV=production
PORT=3000

# Database (Railway will provide this automatically when you add PostgreSQL service)
# DATABASE_URL=postgresql://username:password@host:port/database

# Application Configuration
VITE_BASE_URL=${{RAILWAY_STATIC_URL}}
VITE_PROJECT_GROUP_ID=nivaasi-railway-app
